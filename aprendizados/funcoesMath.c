#include <stdio.h>
#include <math.h>

int main()
{
    
double root = sqrt(16); //Raiz quadrada(square root), output = 4.
double power = pow(4, 2); //potência, output = 16.
int arrendondar = round(3.1415); //output = 3.
int arredondarCima = ceil (3.1415); //arrendonda pra cima, output = 4.
int arrendondarBaixo = floor(3.1415); //arrendonda pra baixo, output = 3.
double absoluto = fabs(-50); //o quão longe esse numero esta de zero, output = 50.
double logaritmo = log(3); //output = 1.098612.
double seno = sin(45); //output o seno de 45.
double cosseno = cos(45); //output o cosseno de 45.
double tangente = tan(45); //output a tangente de 45.

    return 0;
}
